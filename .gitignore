node_modules/
dist/
*.log
.env
.env.*
.DS_Store

# git history generated at runtime by eval fixtures (build-env.ts output)
evals/cases/*/env/.git/

# eval agent artifacts (generated during --run)
evals/cases/*/env/answer.json
.hermes-agent-bridge
agent-bridge

# chita runtime memory (agent-maintained cross-session state, repo-root local only)
.chita/
