{
  "name": "chita-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun build --target=bun ./packages/cli/src/index.ts --outfile ./dist/chita",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
